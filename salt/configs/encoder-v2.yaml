model:
  model:
    class_path: salt.models.SaltModel
    init_args:
      encoder:
        class_path: salt.models.TransformerV2
        init_args:
          embed_dim: 256
          num_layers: 4
          out_dim: 128
          attn_kwargs:
            num_heads: 8
