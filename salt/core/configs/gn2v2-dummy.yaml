# GN2v2 at test scale for the salt dummy-file generators (16-dim, 2 layers,
# 2 heads, 1 register). Trains on a file from
# salt.core.testing.inputs.write_dummy_file with the matching parity norm dict.

name: GN2v2_dummy

data:
  batch_size: 100
  num_workers: 0
  modules:
    reader:
      class_path: salt.core.data.H5StructuredReader
      init_args:
        groups:
          jets: {global_object: true}
          tracks: {global_object: false}
    features:
      class_path: salt.core.data.Features
      init_args:
        variables:
          jets: [pt_btagJes, eta_btagJes]
          tracks:
            [
              d0,
              z0SinTheta,
              dphi,
              deta,
              qOverP,
              IP3D_signed_d0_significance,
              IP3D_signed_z0_significance,
              phiUncertainty,
              thetaUncertainty,
              qOverPUncertainty,
              numberOfPixelHits,
              numberOfSCTHits,
              numberOfInnermostPixelLayerHits,
              numberOfNextToInnermostPixelLayerHits,
              numberOfInnermostPixelLayerSharedHits,
              numberOfInnermostPixelLayerSplitHits,
              numberOfPixelSharedHits,
              numberOfPixelSplitHits,
              numberOfSCTSharedHits,
            ]
    labels:
      class_path: salt.core.data.Labels
      init_args: {dtype_policy: int64-for-int}

model:
  class_path: salt.core.SaltModule
  init_args:
    lrs: {initial: 1.0e-7, max: 1.0e-3, end: 1.0e-5, pct_start: 0.01}
    optimizer: AdamW
    modules:
      norm:
        class_path: salt.core.nn.Normaliser
        init_args:
          # norm_dict must be supplied via CLI/override at fit time.
          streams: [jets, tracks]
          global_object: jets
      track_embed:
        class_path: salt.core.nn.StreamEmbed
        init_args:
          stream: tracks
          context: [normed.jets]
          out_dim: 16
          dense: {hidden_layers: [16], activation: ReLU}
      concat:
        class_path: salt.core.nn.Concat
        init_args: {streams: [tracks]}
      encoder:
        class_path: salt.core.nn.TransformerEncoder
        init_args:
          dim: 16
          out_dim: 16
          num_layers: 2
          attention: {num_heads: 2, attn_type: torch-math}
          dense: {activation: ReLU, gated: false}
      split:
        class_path: salt.core.nn.Split
        init_args: {streams: [tracks]}
      pool:
        class_path: salt.core.nn.GlobalAttentionPooling
        init_args: {input: encoded.seq, out: pooled.global}
      jets_classification:
        class_path: salt.core.nn.tasks.ClassificationTaskModule
        init_args:
          stream: jets
          input: pooled.global
          label: flavour_label
          class_names: [bjets, cjets, ujets]
          dense: {hidden_layers: [16], activation: ReLU}
      track_origin:
        class_path: salt.core.nn.tasks.ClassificationTaskModule
        init_args:
          stream: tracks
          context: pooled.global
          label: ftagTruthOriginLabel
          class_names: [Pileup, Fake, Primary, FromB, FromBC, FromC, FromTau, OtherSecondary]
          weight: 0.5
          dense: {hidden_layers: [16], activation: ReLU}
      track_vertexing:
        class_path: salt.core.nn.tasks.VertexingTaskModule
        init_args:
          stream: tracks
          label: ftagTruthVertexIndex
          origin_label: ftagTruthOriginLabel # declared dependency for origin-weighted loss
          context: pooled.global
          weight: 1.5
          dense: {hidden_layers: [16], activation: ReLU}
          expose: [fit, val] # VertexIndex TEST eval column is deferred
      loss:
        class_path: salt.core.nn.LossSum

# plan 50 Phase B: the eval outputs are declared as an outputs: section (WHAT);
# the command wires the implicit per-command sink (salt2 test -> H5, the run-free
# ONNX parses -> the ONNX tuple). Two RunTaskOutput writers with different modes:
# reproduce the historical explicit-table split — jets_classification is written
# to H5 AND exported to ONNX, track_origin is H5-only (modes: [test]); vertexing
# stays TEST-deferred (expose: [fit, val]) so no VertexIndex column appears.
outputs:
  inputs_copy:
    class_path: salt.core.outputs.InputCopyWriter
    init_args: {streams: [jets, tracks]}
  jets_out:
    class_path: salt.core.outputs.RunTaskOutput
    init_args: {tasks: [jets_classification]} # test + export (both)
  origin_out:
    class_path: salt.core.outputs.RunTaskOutput
    init_args: {tasks: [track_origin], modes: [test]} # eval H5 only
  pad_mask:
    class_path: salt.core.outputs.PadMaskWriter
    init_args: {streams: [tracks]}

export: # inert during fit/test
  model_name: GN2v2dummy # Athena name: no '_'/'-'
  inputs:
    - { port: inputs.jets, name: jet_features }
    - { port: inputs.tracks, name: track_features, sequence: true, dyn_axis: n_tracks }

trainer:
  max_epochs: 2
